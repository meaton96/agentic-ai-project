"""agentic-pdm: predictive-maintenance pipeline where agents write model and
augmentation plugins and a deterministic harness owns everything that has
to stay honest — data loading, folds, normalization, metrics."""

__version__ = "0.1.0"
