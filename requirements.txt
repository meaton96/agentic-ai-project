# Python deps for the whole repo's single root .venv (see README.md
# "Local setup"). Managed with uv:
#
#   uv venv --python 3.12
#   source .venv/bin/activate
#   uv pip install -r requirements.txt
#   uv pip install -e agentic-ml-classification   # editable install so
#                                                  # agent-frontend can
#                                                  # `import agentic_ml`
#
# Covers agentic-ml-classification's pipeline/notebooks and
# agent-frontend's FastAPI server. agent-frontend/web is a separate
# Node project (see its own README) and is not part of this file.

# --- agentic-ml-classification: pipeline + notebooks ---
openai>=1.30.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.4.0
joblib>=1.3.0
lightgbm>=4.0.0
xgboost>=2.0.0
pyarrow>=14.0.0
pytest>=8.0.0
matplotlib>=3.7.0
jupyter>=1.0.0
nbconvert>=7.0.0
nbclient>=0.10.0

# --- agent-frontend: FastAPI server ---
fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0
